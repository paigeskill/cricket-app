{"ast":null,"code":"import deepmerge from '@mui/utils/deepmerge';\nconst focusVisibleOffsetVar = '--_focusVisible-offset';\nconst focusVisibleBehaviorVar = '--_focusVisible-behavior';\nconst focusVisibleShadowVar = '--_focusVisible-shadow';\nconst offsetValue = `var(${focusVisibleOffsetVar}, 1)`;\nconst behaviorValue = `var(${focusVisibleBehaviorVar}, )`;\n\n// Spread on a root whose ring must stay outset: the inset vars inherit, so a clip-prone ancestor\n// would otherwise inset a descendant's ring too.\nexport const outsetFocusRing = {\n  [focusVisibleOffsetVar]: 1,\n  [focusVisibleBehaviorVar]: 'initial' // reverts the var to guaranteed-invalid so `var(--_focusVisible-behavior, )` falls back to empty — there is no explicit `outset` keyword.\n};\n\n// Used by the colored-background surfaces (AppBar, Alert, SnackbarContent) to make the focus visible appear through box-shadow\nexport function applyChildrenFocusVisible(color) {\n  return {\n    [focusVisibleShadowVar]: color\n  };\n}\n\n// Clip-prone components (Tab, MenuItem, …) spread this on their root to inset the ring, so an\n// `overflow: hidden` ancestor cannot clip it — without the component knowing the ring width.\n// `offset` multiplies the ring's own outlineOffset, so 1 = mirror it inward.\nexport function applyInsetFocusVisible(offset) {\n  return {\n    [focusVisibleOffsetVar]: -offset,\n    [focusVisibleBehaviorVar]: 'inset'\n  };\n}\n// Merge the raw `focusVisible` input across `createTheme(options, ...args)` with the theme's own\n// deep-merge semantics. The single-key wrapper is load-bearing — a raw value hits deepmerge's\n// top-level plain-object guard (`deepmerge(true, obj)` drops the object).\nexport function mergeFocusVisibleInput(optionsFocusVisible, args) {\n  return args.reduce((acc, arg) => arg && 'focusVisible' in arg ? deepmerge(acc, {\n    focusVisible: arg.focusVisible\n  }) : acc, {\n    focusVisible: optionsFocusVisible\n  }).focusVisible;\n}\n\n/**\n * Whether an input is this module's own output. Only `wireFocusVisibleVars` emits the offset calc,\n * so it identifies a ring fed back in by `createTheme(existingTheme, …)`.\n */\nexport function isResolvedFocusVisible(input) {\n  return input != null && typeof input === 'object' && typeof input.outlineOffset === 'string' && input.outlineOffset.includes(focusVisibleOffsetVar);\n}\n\n/**\n * Resolve the opt-in ring, wiring in the private inset vars. `outlineColor` is the caller's default\n * — a hex, the palette var, or a scheme's primary — overridden by a user-provided `outlineColor`.\n */\nexport function resolveFocusVisible(input, outlineColor) {\n  return wireFocusVisibleVars({\n    outlineStyle: 'solid',\n    outlineColor,\n    outlineWidth: 2,\n    outlineOffset: 2,\n    // invisible shadow for parent component with solid background (AppBar, Snackbar, Alert) can control the ring color.\n    boxShadow: `var(${focusVisibleShadowVar}, 0 0)`,\n    ...(input === true ? null : input)\n  });\n}\n\n/**\n * Wire the private inset vars into a resolved `theme.focusVisible` so a custom `outlineOffset` or\n * `boxShadow` insets automatically on clip-prone components. Mutates and returns the object.\n */\nexport function wireFocusVisibleVars(resolved) {\n  // Multiply the offset by the sign var so it flips to inset on clip-prone components. Skip if\n  // already wired: re-composing a resolved theme must not wrap the calc twice and invert the sign.\n  const offset = resolved.outlineOffset ?? 0;\n  if (typeof offset !== 'string' || !offset.includes(focusVisibleOffsetVar)) {\n    const offsetPx = typeof offset === 'number' ? `${offset}px` : offset;\n    resolved.outlineOffset = `calc(${offsetValue} * ${offsetPx})`;\n  }\n  // Prefix a box-shadow with the behavior var so it insets there too, unless it already opts in.\n  // Standalone keywords are complete values — prefixing would produce an invalid `inset none`.\n  const standaloneBoxShadows = new Set(['none', 'initial', 'inherit', 'unset', 'revert', 'revert-layer']);\n  if (typeof resolved.boxShadow === 'string' && !standaloneBoxShadows.has(resolved.boxShadow.trim().toLowerCase()) && !/\\binset\\b/.test(resolved.boxShadow) && !resolved.boxShadow.includes(focusVisibleBehaviorVar)) {\n    resolved.boxShadow = `${behaviorValue} ${resolved.boxShadow}`;\n  }\n  return resolved;\n}","map":{"version":3,"names":["deepmerge","focusVisibleOffsetVar","focusVisibleBehaviorVar","focusVisibleShadowVar","offsetValue","behaviorValue","outsetFocusRing","applyChildrenFocusVisible","color","applyInsetFocusVisible","offset","mergeFocusVisibleInput","optionsFocusVisible","args","reduce","acc","arg","focusVisible","isResolvedFocusVisible","input","outlineOffset","includes","resolveFocusVisible","outlineColor","wireFocusVisibleVars","outlineStyle","outlineWidth","boxShadow","resolved","offsetPx","standaloneBoxShadows","Set","has","trim","toLowerCase","test"],"sources":["/Users/paigecox/cricket-app/node_modules/@mui/material/styles/focusVisible.mjs"],"sourcesContent":["import deepmerge from '@mui/utils/deepmerge';\nconst focusVisibleOffsetVar = '--_focusVisible-offset';\nconst focusVisibleBehaviorVar = '--_focusVisible-behavior';\nconst focusVisibleShadowVar = '--_focusVisible-shadow';\nconst offsetValue = `var(${focusVisibleOffsetVar}, 1)`;\nconst behaviorValue = `var(${focusVisibleBehaviorVar}, )`;\n\n// Spread on a root whose ring must stay outset: the inset vars inherit, so a clip-prone ancestor\n// would otherwise inset a descendant's ring too.\nexport const outsetFocusRing = {\n  [focusVisibleOffsetVar]: 1,\n  [focusVisibleBehaviorVar]: 'initial' // reverts the var to guaranteed-invalid so `var(--_focusVisible-behavior, )` falls back to empty — there is no explicit `outset` keyword.\n};\n\n// Used by the colored-background surfaces (AppBar, Alert, SnackbarContent) to make the focus visible appear through box-shadow\nexport function applyChildrenFocusVisible(color) {\n  return {\n    [focusVisibleShadowVar]: color\n  };\n}\n\n// Clip-prone components (Tab, MenuItem, …) spread this on their root to inset the ring, so an\n// `overflow: hidden` ancestor cannot clip it — without the component knowing the ring width.\n// `offset` multiplies the ring's own outlineOffset, so 1 = mirror it inward.\nexport function applyInsetFocusVisible(offset) {\n  return {\n    [focusVisibleOffsetVar]: -offset,\n    [focusVisibleBehaviorVar]: 'inset'\n  };\n}\n// Merge the raw `focusVisible` input across `createTheme(options, ...args)` with the theme's own\n// deep-merge semantics. The single-key wrapper is load-bearing — a raw value hits deepmerge's\n// top-level plain-object guard (`deepmerge(true, obj)` drops the object).\nexport function mergeFocusVisibleInput(optionsFocusVisible, args) {\n  return args.reduce((acc, arg) => arg && 'focusVisible' in arg ? deepmerge(acc, {\n    focusVisible: arg.focusVisible\n  }) : acc, {\n    focusVisible: optionsFocusVisible\n  }).focusVisible;\n}\n\n/**\n * Whether an input is this module's own output. Only `wireFocusVisibleVars` emits the offset calc,\n * so it identifies a ring fed back in by `createTheme(existingTheme, …)`.\n */\nexport function isResolvedFocusVisible(input) {\n  return input != null && typeof input === 'object' && typeof input.outlineOffset === 'string' && input.outlineOffset.includes(focusVisibleOffsetVar);\n}\n\n/**\n * Resolve the opt-in ring, wiring in the private inset vars. `outlineColor` is the caller's default\n * — a hex, the palette var, or a scheme's primary — overridden by a user-provided `outlineColor`.\n */\nexport function resolveFocusVisible(input, outlineColor) {\n  return wireFocusVisibleVars({\n    outlineStyle: 'solid',\n    outlineColor,\n    outlineWidth: 2,\n    outlineOffset: 2,\n    // invisible shadow for parent component with solid background (AppBar, Snackbar, Alert) can control the ring color.\n    boxShadow: `var(${focusVisibleShadowVar}, 0 0)`,\n    ...(input === true ? null : input)\n  });\n}\n\n/**\n * Wire the private inset vars into a resolved `theme.focusVisible` so a custom `outlineOffset` or\n * `boxShadow` insets automatically on clip-prone components. Mutates and returns the object.\n */\nexport function wireFocusVisibleVars(resolved) {\n  // Multiply the offset by the sign var so it flips to inset on clip-prone components. Skip if\n  // already wired: re-composing a resolved theme must not wrap the calc twice and invert the sign.\n  const offset = resolved.outlineOffset ?? 0;\n  if (typeof offset !== 'string' || !offset.includes(focusVisibleOffsetVar)) {\n    const offsetPx = typeof offset === 'number' ? `${offset}px` : offset;\n    resolved.outlineOffset = `calc(${offsetValue} * ${offsetPx})`;\n  }\n  // Prefix a box-shadow with the behavior var so it insets there too, unless it already opts in.\n  // Standalone keywords are complete values — prefixing would produce an invalid `inset none`.\n  const standaloneBoxShadows = new Set(['none', 'initial', 'inherit', 'unset', 'revert', 'revert-layer']);\n  if (typeof resolved.boxShadow === 'string' && !standaloneBoxShadows.has(resolved.boxShadow.trim().toLowerCase()) && !/\\binset\\b/.test(resolved.boxShadow) && !resolved.boxShadow.includes(focusVisibleBehaviorVar)) {\n    resolved.boxShadow = `${behaviorValue} ${resolved.boxShadow}`;\n  }\n  return resolved;\n}"],"mappings":"AAAA,OAAOA,SAAS,MAAM,sBAAsB;AAC5C,MAAMC,qBAAqB,GAAG,wBAAwB;AACtD,MAAMC,uBAAuB,GAAG,0BAA0B;AAC1D,MAAMC,qBAAqB,GAAG,wBAAwB;AACtD,MAAMC,WAAW,GAAG,OAAOH,qBAAqB,MAAM;AACtD,MAAMI,aAAa,GAAG,OAAOH,uBAAuB,KAAK;;AAEzD;AACA;AACA,OAAO,MAAMI,eAAe,GAAG;EAC7B,CAACL,qBAAqB,GAAG,CAAC;EAC1B,CAACC,uBAAuB,GAAG,SAAS,CAAC;AACvC,CAAC;;AAED;AACA,OAAO,SAASK,yBAAyBA,CAACC,KAAK,EAAE;EAC/C,OAAO;IACL,CAACL,qBAAqB,GAAGK;EAC3B,CAAC;AACH;;AAEA;AACA;AACA;AACA,OAAO,SAASC,sBAAsBA,CAACC,MAAM,EAAE;EAC7C,OAAO;IACL,CAACT,qBAAqB,GAAG,CAACS,MAAM;IAChC,CAACR,uBAAuB,GAAG;EAC7B,CAAC;AACH;AACA;AACA;AACA;AACA,OAAO,SAASS,sBAAsBA,CAACC,mBAAmB,EAAEC,IAAI,EAAE;EAChE,OAAOA,IAAI,CAACC,MAAM,CAAC,CAACC,GAAG,EAAEC,GAAG,KAAKA,GAAG,IAAI,cAAc,IAAIA,GAAG,GAAGhB,SAAS,CAACe,GAAG,EAAE;IAC7EE,YAAY,EAAED,GAAG,CAACC;EACpB,CAAC,CAAC,GAAGF,GAAG,EAAE;IACRE,YAAY,EAAEL;EAChB,CAAC,CAAC,CAACK,YAAY;AACjB;;AAEA;AACA;AACA;AACA;AACA,OAAO,SAASC,sBAAsBA,CAACC,KAAK,EAAE;EAC5C,OAAOA,KAAK,IAAI,IAAI,IAAI,OAAOA,KAAK,KAAK,QAAQ,IAAI,OAAOA,KAAK,CAACC,aAAa,KAAK,QAAQ,IAAID,KAAK,CAACC,aAAa,CAACC,QAAQ,CAACpB,qBAAqB,CAAC;AACrJ;;AAEA;AACA;AACA;AACA;AACA,OAAO,SAASqB,mBAAmBA,CAACH,KAAK,EAAEI,YAAY,EAAE;EACvD,OAAOC,oBAAoB,CAAC;IAC1BC,YAAY,EAAE,OAAO;IACrBF,YAAY;IACZG,YAAY,EAAE,CAAC;IACfN,aAAa,EAAE,CAAC;IAChB;IACAO,SAAS,EAAE,OAAOxB,qBAAqB,QAAQ;IAC/C,IAAIgB,KAAK,KAAK,IAAI,GAAG,IAAI,GAAGA,KAAK;EACnC,CAAC,CAAC;AACJ;;AAEA;AACA;AACA;AACA;AACA,OAAO,SAASK,oBAAoBA,CAACI,QAAQ,EAAE;EAC7C;EACA;EACA,MAAMlB,MAAM,GAAGkB,QAAQ,CAACR,aAAa,IAAI,CAAC;EAC1C,IAAI,OAAOV,MAAM,KAAK,QAAQ,IAAI,CAACA,MAAM,CAACW,QAAQ,CAACpB,qBAAqB,CAAC,EAAE;IACzE,MAAM4B,QAAQ,GAAG,OAAOnB,MAAM,KAAK,QAAQ,GAAG,GAAGA,MAAM,IAAI,GAAGA,MAAM;IACpEkB,QAAQ,CAACR,aAAa,GAAG,QAAQhB,WAAW,MAAMyB,QAAQ,GAAG;EAC/D;EACA;EACA;EACA,MAAMC,oBAAoB,GAAG,IAAIC,GAAG,CAAC,CAAC,MAAM,EAAE,SAAS,EAAE,SAAS,EAAE,OAAO,EAAE,QAAQ,EAAE,cAAc,CAAC,CAAC;EACvG,IAAI,OAAOH,QAAQ,CAACD,SAAS,KAAK,QAAQ,IAAI,CAACG,oBAAoB,CAACE,GAAG,CAACJ,QAAQ,CAACD,SAAS,CAACM,IAAI,CAAC,CAAC,CAACC,WAAW,CAAC,CAAC,CAAC,IAAI,CAAC,WAAW,CAACC,IAAI,CAACP,QAAQ,CAACD,SAAS,CAAC,IAAI,CAACC,QAAQ,CAACD,SAAS,CAACN,QAAQ,CAACnB,uBAAuB,CAAC,EAAE;IAClN0B,QAAQ,CAACD,SAAS,GAAG,GAAGtB,aAAa,IAAIuB,QAAQ,CAACD,SAAS,EAAE;EAC/D;EACA,OAAOC,QAAQ;AACjB","ignoreList":[]},"metadata":{},"sourceType":"module","externalDependencies":[]}